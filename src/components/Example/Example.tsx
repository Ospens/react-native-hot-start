import React, { memo } from "react";
import { View, Text } from "react-native";

const Example = memo(() => {
  return (
    <View>
      <Text>Example</Text>
    </View>
  );
});

export default Example;
